export enum TransactionStatus {
  Pending = "pending",
  Success = "success",
  Failed = "failed",
}

export enum TransactionType {
  Deposit = "deposit",
  Payment = "payment",
  Withdraw = "withdraw",
}
