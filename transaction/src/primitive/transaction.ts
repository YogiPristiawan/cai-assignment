export enum TransactionStatus {
  Pending = "pending",
  Success = "success",
  Failed = "failed",
}

export enum TransactionAccountType {
  Credit = "credit",
  Debit = "debit",
  Loan = "loan",
}

export enum TransactionType {
  Deposit = "deposit",
  Payment = "payment",
  Withdraw = "withdraw",
}
